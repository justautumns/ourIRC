1.) I added in Server.cpp -> removeClient function, before removing the client I make sure client leaves all the channels it's in.
if it's in any channel.
2.) I added in Channel.cpp -> removeUser function, If the user in operators array we delete the user from that list too. 
when user were parting the channel it was still list in ops array.

3.) I added in ChannelCommands.cpp -> I set the channel pointer according to cmd. 
4.) When a new client joins the channel after the topic was changed. He doesn't see topic msg clearly. Instead of 
channel name he sees topic msg you can try. I left a comment where it should be fixed for now.
I think replie was not good formatted we need the topic setter and time blabla according to IRC replies
ie : RPL_TOPICWHOTIME 333